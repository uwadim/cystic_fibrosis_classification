# Where the dataset should be stored. This folder is split into raw_dir (downloaded dataset)
# and processed_dir (processed data).
root_directory: '${hydra:runtime.cwd}/../data'
# Path to the network data
network_fpath: '${hydra:runtime.cwd}/../data/raw/сystic_fibrosis_network_symmetrical_11_01_2023.csv'
# Path to the original data with labels
original_fpath: '${hydra:runtime.cwd}/../data/сystic_fibrosis_11_01_2023 - сystic_fibrosis_11_01_2023.csv.csv'
# Name of column that describes different patients
id_colname: 'Sample Name'
# Name of column that describes Label (target)
label_colname: 'Group'
